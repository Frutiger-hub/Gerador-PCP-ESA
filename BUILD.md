# Build Windows

O projeto é empacotado no GitHub Actions usando Python 3.14 e PyInstaller. O workflow reconstrói o aplicativo para Windows, inclui os ícones e grava o EXE em `dist/Gerador_PCP_ESA.exe`.
